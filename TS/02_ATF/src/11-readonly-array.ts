const numbers: ReadonlyArray<number> = [1,2,5,6]

// numbers.push(12)
// numbers.pop()
// numbers.unshift(1)
numbers.filter(()=>{}) //solo me deja hacer filter
numbers.reduce(()=>0)
numbers.map(()=>0)

console.log(numbers)
